# Улучшения гистограммы

## Проблема
При повторной загрузке одной и той же текстуры значения гистограммы **изменялись**, что делало данные ненадежными для анализа.

## Причина
**Race condition** в методе `ProcessImage()` - несколько потоков одновременно инкрементировали один и тот же элемент массива гистограммы:
```csharp
// ❌ Небезопасно в Parallel.For!
redHistogram[pixel.R]++;
```

## Решение

### 1. Исправлен race condition
**Файл:** `Helpers/MainWindowHelpers.cs:272-303`

Реализована thread-local стратегия:
- Каждый поток создает свою локальную гистограмму
- После обработки локальные гистограммы объединяются с блокировкой
- Гарантирует точные и стабильные результаты

```csharp
Parallel.For(0, image.Height, () => {
    // Thread-local initialization
    return (Red: new int[256], Green: new int[256], Blue: new int[256]);
},
(y, loopState, localHistograms) => {
    // Process with local histograms
    // ...
    return localHistograms;
},
localHistograms => {
    // Merge with lock
    lock (lockObject) {
        for (int i = 0; i < 256; i++) {
            redHistogram[i] += localHistograms.Red[i];
            // ...
        }
    }
});
```

### 2. Добавлена статистика гистограммы
**Файл:** `Helpers/MainWindowHelpers.cs:305-370`

Новый класс `HistogramStatistics` вычисляет:
- **Min** - минимальное значение яркости
- **Max** - максимальное значение яркости
- **Mean** - среднее значение (взвешенное)
- **Median** - медиана распределения
- **StdDev** - стандартное отклонение (контраст)
- **TotalPixels** - общее количество пикселей

### 3. UI для отображения статистики
**Файл:** `MainWindow.xaml:550-586`

Добавлена панель под графиком гистограммы с компактным отображением всех статистических параметров в формате 2x3 таблицы.

### 4. Интеграция в методы обновления
**Файлы:**
- `MainWindow.xaml.cs:1267-1317` - `UpdateHistogram()`
- `MainWindow.xaml.cs:1319-1378` - `UpdateHistogramAsync()` + `UpdateHistogramStatisticsUI()`

Оба метода теперь:
- Вычисляют комбинированную гистограмму для статистики
- Вызывают `CalculateHistogramStatistics()`
- Обновляют UI через `UpdateHistogramStatisticsUI()`

## Преимущества

### Точность
✅ Гистограмма теперь **детерминирована** - одна и та же текстура дает одинаковые результаты
✅ Нет потери данных из-за race conditions
✅ Точные подсчеты пикселей

### Информативность
📊 **Min/Max** - показывают диапазон яркости (экспозиция)
📊 **Mean/Median** - центр распределения (общая яркость)
📊 **StdDev** - контраст изображения
📊 **Pixels** - размер выборки для проверки

### Производительность
⚡ Thread-local подход быстрее Interlocked
⚡ Минимальная блокировка (только при объединении)
⚡ Сохранена параллельная обработка

## Применение

### Анализ экспозиции
- **Mean < 85** - темное изображение
- **Mean 85-170** - нормальная экспозиция
- **Mean > 170** - пересвет

### Анализ контраста
- **StdDev < 30** - низкий контраст (плоское)
- **StdDev 30-70** - нормальный контраст
- **StdDev > 70** - высокий контраст

### Проверка данных
- **Pixels** должно равняться `Width × Height × 3` (RGB)
- **Min = Max** означает одноцветное изображение
- **Median ≈ Mean** - симметричное распределение

## Тестирование

Для проверки исправления:
1. Загрузите текстуру
2. Запомните значения статистики
3. Переключите на другую текстуру
4. Вернитесь к первой текстуре
5. **Результат:** значения должны быть идентичны

## Технические детали

### Формула статистик
- **Mean:** `Σ(value × count) / total_pixels`
- **Median:** накопительная сумма до 50% пикселей
- **StdDev:** `√(Σ((value - mean)² × count) / total_pixels)`

### Комбинированная гистограмма
Для статистики используется сумма всех каналов (не luminance):
```csharp
combinedHistogram[i] = redHistogram[i] + greenHistogram[i] + blueHistogram[i];
```
Это дает статистику по всем пикселям всех каналов.
