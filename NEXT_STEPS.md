# Следующие шаги для диагностики проблемы с toktx

## ✅ Что было сделано:

1. **Актуализированы настройки текстур** - обновлена система ColorSpace, ToktxMipFilter, WrapMode
2. **Исправлен пресет Normal Map** - отключены конфликтующие флаги ConvertToNormalMap и NormalizeVectors
3. **Улучшена логика ToktxWrapper** - флаги --normal_mode и --normalize применяются только для single image mode
4. **Добавлено детальное логирование** - версия toktx, полная командная строка, все аргументы

## 📋 Что нужно сделать:

### Шаг 1: Перезапустите TexTool

1. **Закройте TexTool** если он запущен
2. **Соберите проект заново**:
   ```bash
   git pull
   dotnet build TexTool.sln --configuration Release
   ```
3. **Запустите TexTool** из `bin/Release/net9.0-windows10.0.26100.0/TexTool.exe`

### Шаг 2: Попробуйте конвертировать текстуру

1. Откройте **Texture Conversion Window**
2. Выберите текстуру `well_normal`
3. Убедитесь что выбран пресет **"Normal (Linear)"**
4. Нажмите **Convert**

### Шаг 3: Соберите логи

**Если ошибка повторяется:**

1. Найдите файл **`file.txt`** в директории где запущен TexTool
   - Обычно это: `bin/Release/net9.0-windows10.0.26100.0/file.txt`
   - Или поищите в корне проекта

2. Откройте `file.txt` и найдите последнюю секцию `=== TOKTX COMMAND ===`

3. **Скопируйте следующие секции:**
   ```
   === TOKTX PACKING START ===
     toktx version: ...
     Mipmaps count: ...
     ...

   === TOKTX COMMAND ===
     Executable: toktx
     Full command line:
     ...
     Individual arguments:
     [0] = ...
     [1] = ...
     ...
   ```

4. **Также скопируйте секцию ошибки:**
   ```
   [toktx ERROR] ...
   ```

### Шаг 4: Проверьте версию toktx

Откройте командную строку и выполните:
```bash
toktx --version
```

**Скопируйте вывод.**

### Шаг 5: Попробуйте выполнить команду вручную

1. Из логов скопируйте строку **"Full command line:"**
2. Замените относительные пути на абсолютные (если нужно)
3. Выполните команду в командной строке
4. **Скопируйте результат** (успех или ошибку)

**Пример:**
```bash
toktx --t2 --assign_oetf linear --encode uastc --uastc_quality 3 --uastc_rdo_l 1.0 --zcmp 3 --wmode clamp --mipmap --levels 5 C:/output.ktx2 C:/mip0.png C:/mip1.png ...
```

### Шаг 6: Предоставьте информацию

Создайте ответ с следующей информацией:

```
## Версия toktx:
[вставьте вывод toktx --version]

## Логи из file.txt:
[вставьте секции === TOKTX PACKING START === и === TOKTX COMMAND ===]

## Результат выполнения вручную:
[вставьте результат выполнения команды]

## Операционная система:
[например: Windows 11, Windows 10, etc.]
```

---

## 🔧 Альтернативный вариант: Упрощенный пресет

Если хотите быстро проверить работает ли toktx вообще, создайте упрощенный пресет:

1. Откройте **Preset Management**
2. Создайте новый пресет:
   - **Name:** "Test Simple"
   - **Compression Format:** UASTC
   - **Output Format:** KTX2
   - **UASTC Quality:** 2
   - **Use UASTC RDO:** ❌ **false** (важно!)
   - **Color Space:** **Auto** (не Linear!)
   - **KTX2 Supercompression:** **None** (не Zstandard!)
   - **Generate Mipmaps:** true
   - **Mip Filter:** Kaiser
   - Остальное: по умолчанию

3. Попробуйте конвертировать с этим пресетом

---

## 📚 Полезные ссылки:

- **Troubleshooting guide:** `TROUBLESHOOTING_TOKTX.md`
- **Normal maps guide:** `TextureConversion/NORMAL_MAPS_GUIDE.md`
- **toktx documentation:** https://github.khronos.org/KTX-Software/ktxtools/toktx.html

---

## 🐛 Возможные причины проблемы:

1. **Старая версия toktx** - требуется v4.3.0 или выше
2. **Несовместимые флаги** - некоторые флаги могут конфликтовать
3. **Проблема с путями** - пути с пробелами или недопустимые символы
4. **Отсутствие файлов** - временные мипмапы могли быть удалены
5. **Ограничения ОС** - антивирус или ограничения прав доступа

После предоставления логов смогу точно определить причину! 🔍
