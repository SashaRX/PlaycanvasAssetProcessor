<Window x:Class="AssetProcessor.Windows.MasterMaterialEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Master Material Editor"
        Height="500" Width="600"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ThemeBackground}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid x:Name="RootGrid" Background="{DynamicResource ThemeBackground}" TextElement.Foreground="{DynamicResource ThemeForeground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: Basic Info -->
        <Grid x:Name="HeaderGrid" Grid.Row="0" Margin="10" Background="{DynamicResource ThemeBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"
                       Foreground="{DynamicResource ThemeForeground}"/>
            <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,5"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Description:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"
                       Foreground="{DynamicResource ThemeForeground}"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5"
                     Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Base Material ID:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"
                       Foreground="{DynamicResource ThemeForeground}"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,5"
                     Text="{Binding BaseMaterialIdText, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Blend Type:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"
                       Foreground="{DynamicResource ThemeForeground}"/>
            <ComboBox Grid.Row="3" Grid.Column="1" Margin="0,5"
                      ItemsSource="{Binding BlendTypes}"
                      SelectedValue="{Binding BlendType}"/>
        </Grid>

        <!-- Chunks List -->
        <Grid Grid.Row="1" Margin="10,0,10,10" Background="{DynamicResource ThemeBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Attached Chunks:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"
                           Foreground="{DynamicResource ThemeForeground}"/>
                <Button Content="+ Add Chunk" Click="AddChunk_Click" Width="100" Margin="0,0,5,0"/>
                <Button Content="Remove" Click="RemoveChunk_Click" Width="80"/>
            </StackPanel>

            <ListBox Grid.Row="1" x:Name="ChunksListBox"
                     ItemsSource="{Binding AttachedChunks}"
                     SelectedItem="{Binding SelectedChunk}"
                     DisplayMemberPath="ChunkName"
                     Background="{DynamicResource ThemeBackgroundAlt}"
                     Foreground="{DynamicResource ThemeForeground}"/>
        </Grid>

        <!-- Footer: Buttons -->
        <Border x:Name="FooterGrid" Grid.Row="2" Background="{DynamicResource ThemeBackground}">
            <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="UnsavedIndicator" Text="* Unsaved changes"
                           Foreground="Orange" VerticalAlignment="Center" Margin="0,0,20,0"
                           Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <Button Content="Reset" Click="ResetButton_Click" Width="80" Margin="0,0,10,0"
                        IsEnabled="{Binding HasUnsavedChanges}"/>
                <Button Content="Save" Click="SaveButton_Click" Width="80" Margin="0,0,10,0"
                        Background="#FF4CAF50" Foreground="White"/>
                <Button Content="Cancel" Click="CancelButton_Click" Width="80" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
